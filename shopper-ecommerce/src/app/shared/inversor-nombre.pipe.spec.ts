import { InversorNombrePipe } from './inversor-nombre.pipe';

describe('InversorNombrePipe', () => {
  it('create an instance', () => {
    const pipe = new InversorNombrePipe();
    expect(pipe).toBeTruthy();
  });
});
